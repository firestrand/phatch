# Phatch Runtime Dependencies
# Install with: pip install -r requirements.txt

# Image processing library (replaces legacy PIL)
Pillow>=11.3.0

# Cross-platform GUI framework
wxPython>=4.2.3

# Optional: EXIF metadata support
# pyexiv2>=2.8.0  # Uncomment if needed - may require system packages
